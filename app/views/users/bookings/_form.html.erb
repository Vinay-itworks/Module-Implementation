<%= form_with(model: [user, booking]) do |form| %>
  <% if booking.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
        <% booking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :events_id, style: "display: block" %>
    <%= form.text_field :events_id %>
  </div>

  <div>
    <%= form.label :ticket, style: "display: block" %>
    <%= form.text_field :ticket %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.number_field :price %>
  </div>

  <div>
    <%= form.label :users_id, style: "display: block" %>
    <%= form.text_field :users_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
